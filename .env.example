# Example environment file. Copy this to `.env` and fill in real values.
# `.env` is gitignored — never commit secrets.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-or-publishable-key